[package]
name = "peekoo-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
peekoo-paths = { path = "../peekoo-paths" }
pi = { version = "0.1.7", package = "pi_agent_rust" }
serde_json = "1"

[dev-dependencies]
asupersync = "=0.2.5"
