[package]
name = "peekoo-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
pi = { version = "0.1.7", package = "pi_agent_rust" }
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
