[package]
name = "peekoo-notifications"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
tokio = { version = "1.50.0", features = ["sync"] }
tracing = "0.1"
