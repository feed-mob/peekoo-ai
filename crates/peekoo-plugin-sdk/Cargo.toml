[package]
name = "peekoo-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for building Peekoo desktop-pet plugins"

[workspace]

[lib]
crate-type = ["rlib"]

[dependencies]
extism-pdk = "1.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
