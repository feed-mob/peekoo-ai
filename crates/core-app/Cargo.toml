[package]
name = "peekoo-core-app"
version = "0.1.0"
edition = "2024"

[dependencies]
peekoo-core-domain = { path = "../core-domain" }
peekoo-event-bus = { path = "../event-bus" }
peekoo-agent = { path = "../peekoo-agent" }
serde_json = "1"
